{
  "name": "obsidian-multi-find-replace",
  "version": "0.1.0",
  "description": "Multi-file find and replace plugin for Obsidian",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "vitest run"
  },
  "devDependencies": {
    "obsidian": "latest",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "vitest": "^1.0.0",
    "esbuild": "^0.20.0"
  },
  "dependencies": {
    "ignore": "^5.3.0"
  }
}